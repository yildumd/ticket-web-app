{
  "name": "ticket-app-twig",
  "description": "Ticket management app - Twig/PHP version",
  "require": {
    "twig/twig": "^3.0",
    "symfony/http-foundation": "^6.0",
    "symfony/routing": "^6.0",
    "symfony/http-kernel": "^6.0"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  },
  "scripts": {
    "serve": "php -S localhost:8000 -t public"
  }
}